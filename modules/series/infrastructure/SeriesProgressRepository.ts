// modules/series/infrastructure/SeriesProgressRepository.ts
export class SeriesProgressRepository {
  async saveProgress() {
    // aquí luego usarás Prisma / MySQL
  }

  async getLastProgress(/* params */) {
    // consulta a la DB
  }
}
